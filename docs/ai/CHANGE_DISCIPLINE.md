# Change Discipline

Stop adding scope once a branch satisfies its stated risk reduction. Open the PR,
let the exact candidate run through the validation order, and fix only findings
that belong to that scope. Follow-up architecture phases use separate branches.
